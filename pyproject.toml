[project]
name = "ml-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yeongmin Lee", email = "munain0417@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
    "ruff>=0.16.6",
    "torch>=2.14.0",
]

[project.scripts]
ml-lab = "ml_lab:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
